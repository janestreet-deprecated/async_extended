(executables
 ((names (find zcat run_in_fork))
  (libraries (
    async
    async_extended
    core
    pcre
    reader_extended))
  (preprocess (pps (ppx_jane ppxlib.runner)))))


(jbuild_version 1)
